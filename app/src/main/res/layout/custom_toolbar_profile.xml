<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/customToolbarContainerAddContact"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/actionBarSize"
    android:background="@color/purple_color">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/imgBackToolbarProfile"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/custom_toolbar_profile_img_back_height"
        android:padding="@dimen/custom_toolbar_profile_img_back_padding"
        android:src="@drawable/ic_arrow_back"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tvToolbarAddContact"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/custom_toolbar_profile_textview_text"
        android:fontFamily="@font/opensans_semibold_600"
        android:textSize="@dimen/custom_toolbar_profile_text_size"
        android:textColor="@color/white_color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/guideline"
        app:layout_constraintStart_toStartOf="@+id/guideline"
        app:layout_constraintTop_toTopOf="parent"/>

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_percent="@dimen/custom_toolbar_profile_guideline_position" />

</androidx.constraintlayout.widget.ConstraintLayout>